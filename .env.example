#
# Quilox Auth API Environment Variables
#
# This file stores configuration settings for your application,
# keeping sensitive data out of the source code.
#

# -------------------------
# 1. APPLICATION SETTINGS
# -------------------------

# The port on which the Express server will run.
# Change this to a different port if 5000 is in use.
PORT=5000

# -------------------------
# 2. DATABASE SETTINGS
# -------------------------

# The MongoDB connection URI.
# Replace <username>, <password>, and <cluster-name> with your own credentials.
# The `?retryWrites=true&w=majority` part is standard for MongoDB Atlas.
MONGO_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/my-express-app?retryWrites=true&w=majority
# -------------------------
# 3. JSON WEB TOKEN (JWT) SETTINGS
# -------------------------

# A secret key used to sign and verify JWTs.
# Generate a strong, random string for this. Do not share it.
JWT_SECRET="your_very_secret_and_long_jwt_secret_key"

# The expiration time for authentication tokens.
# Use a format like '1h' (1 hour), '2d' (2 days), etc.
JWT_EXPIRES_IN="1d"

# -------------------------
# 4. PASSWORD HASHING SETTINGS
# -------------------------

# The salt rounds for bcrypt.
# A higher number increases security but also processing time.
# A value of 10-12 is generally a good balance.
BCRYPT_SALT_ROUNDS=10